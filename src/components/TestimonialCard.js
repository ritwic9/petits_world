import React from "react";

const TestimonialCard = ({ name, entry }) => {
  return (
    <div>
      <span>"</span>
      <span>{name}</span>
      <p>{entry}</p>
    </div>
  );
};

export default TestimonialCard;
